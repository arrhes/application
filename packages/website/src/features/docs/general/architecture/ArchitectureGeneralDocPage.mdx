# Architecture

Architecture

Vue d'ensemble de l'architecture technique d'Arrhes et de ses différents services.

Arrhes est organisé en plusieurs services distincts qui travaillent ensemble. Chaque service a un

Tous les services passent par l'API, qui est le cœur du système. C'est elle qui

Le dashboard est l'interface web d'Arrhes. Il permet de gérer ses organisations, saisir ses

écritures comptables, consulter ses documents de synthèse, stocker ses pièces justificatives et

Pour qui ? Les utilisateurs qui souhaitent gérer leur comptabilité au quotidien

Le CLI est une interface en ligne de commande qui donne accès aux mêmes fonctionnalités que le

Pour qui ? Les développeurs et équipes techniques qui souhaitent automatiser des

L'API est le service central d'Arrhes. Elle reçoit toutes les requêtes - qu'elles viennent du

Pour qui ? Les développeurs qui souhaitent accéder directement aux données d'Arrhes

La base de données stocke l'ensemble des données d'Arrhes : utilisateurs, organisations, exercices,

écritures comptables, fichiers et sessions. Elle est le seul endroit où les données sont persistées

Le stockage des fichiers (pièces justificatives, documents exportés est géré séparément, dans un

Pour qui ? Ce service est entièrement géré par Arrhes. Les utilisateurs et
