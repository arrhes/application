# Authentication Cli

Le CLI vérifie la clé auprès de l'API, puis enregistre la configuration dans

~/.arrhes/config.json.

L'identifiant d'organisation (--org est requis - il est automatiquement injecté

L'option --url est facultative. Par défaut, le CLI se connecte à

Pour effacer les identifiants stockés localement :

Les identifiants sont stockés en clair dans ~/.arrhes/config.json. Vous pouvez

éditer ce fichier directement si nécessaire.

Pour utiliser plusieurs comptes ou environnements, il suffit de relancer

En savoir plus sur l'authentification par clé API
